# Image evaluator script specifications
_Exported on 3/9/2026 at 09:28:16 PDT from Cursor (2.6.13)_

---

**User**

Generate an second image evaluator script, `image_evaluator_nima.py` that will evaluate images that are 4080x3071 pixels, stored as jpeg files of 2 to 4mb each. As input the script will take directory path. It will then look at each picture in the main and subdirectories. At the end it will save detailed info on the pictures in the directory path in a .json file. Information will include a relative path to the image file, detailed image evaluation results and time taken to evaluate the picture. Also include a total run time for all images, number of images read, basic stats for picture run times and scoring results. If run again on the same directory path, only process those images not previously processed. For image evaluation use NIMA. Write the .json file after each image is evaluated so that if the process is stopped no data is lost.

---

**Cursor**


Exploring the project structure and any existing image evaluator to learn patterns, then creating the NIMA-based image evaluator script.



Checking the project's requirements and how we can integrate NIMA (Neural Image Assessment):



Creating the NIMA-based image evaluator script and adding a requirements file for it.



Fixing device detection (pyiqa may not expose `torch`), ensuring we save when there are no new images, and setting total run time to the sum of per-image evaluation times.



Using the sum of all records' evaluation times as total run time (not just this run). Updating save logic accordingly:



Adding a requirements file for the NIMA evaluator and checking pyiqa's NIMA output format.



Handling NumPy scalars in the NIMA output (pyiqa may return a 0-d array):



Summary of what’s in place:

### `image_evaluator_nima.py`

- **Input:** Single directory path (positional). Recursively finds JPEGs in that directory and subdirectories.
- **Output:** Writes to `<directory>/image_evaluation_nima_results.json` (override with `--output`). The JSON is **rewritten after each image** so interrupting the run doesn’t lose already-evaluated images.
- **Per-image record:** For each image it stores:
  - `relative_path` – path relative to the root directory  
  - `file_size_bytes`, `file_size_mb`, `width`, `height`  
  - `evaluated_at` – ISO timestamp  
  - `evaluation_time_seconds` – time for that image  
  - `nima_score`, `nima_details`, `nima_error` – NIMA result (or error message)
- **Summary section:** At the top level it also writes:
  - `total_images` – number of images in the results list  
  - `total_run_time_seconds` – sum of all `evaluation_time_seconds`  
  - `stats` – for **evaluation_times_seconds** and **nima_scores**: `count`, `min`, `max`, `mean`, `std`
- **Resume behavior:** Loads the existing JSON if present and **skips any image whose `relative_path` is already in the results**. Only new images are run through NIMA; then the file is updated and written after each new image.
- **NIMA:** Uses the **pyiqa** library (`pyiqa.create_metric('nima', device=...)`). First run may download NIMA weights (e.g. from Hugging Face).

### Usage

```bash
# Install deps
pip install -r requirements_image_evaluator_nima.txt

# Run (output: <directory>/image_evaluation_nima_results.json)
python image_evaluator_nima.py /path/to/images

# Custom output path
python image_evaluator_nima.py /path/to/images --output /path/to/my_nima_results.json

# Use GPU
python image_evaluator_nima.py /path/to/images --device cuda
```

### `requirements_image_evaluator_nima.txt`

- `Pillow`, `torch`, `torchvision`, `pyiqa`

If NIMA model download fails (e.g. Hugging Face blocked), you can use a mirror:  
`export HF_ENDPOINT=https://hf-mirror.com` before running.

