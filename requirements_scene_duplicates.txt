# Scene duplicates by score (scene_duplicates_by_score.py)
# Install: pip install -r requirements_scene_duplicates.txt
# Also requires image_evaluator dependencies so scores exist (see requirements_image_evaluator.txt).

imagededup>=0.3.0
numpy>=1.20.0
# exifread: EXIF (GPS, datetime) using tag names from process_images/image_collection_metadata.csv
exifread>=3.0.0
